# Linux Basic Commands
## Working with files 

**Author:** ApostolR1 
**Date:** 2026-04-19

---

## 1. touch try.txt

### Description 
The `touch try.txt` command witch used for create **text file** with name try.txt

### Command
```bash
touch 

```
![typing command in the terminal](touch.png)

## 2. mcdir lessons 

### Description 
The `mcdir lessons` command for create **directive** lessons

### Command
```bash
mcdir

```
![typing command in the terminal](mcdir.png)

## 3. rm try.txt 

### Description 
The `rm try.txt` command for **delete** file, in this case i deleted try.txt

### Command
```bash
rm

```
![typing command in the terminal](rm.png)

## 4. rm -r lessons 

### Description 
The `rm -r` command for *delete* **full derective** with files inside, in this example i deleted folder lessons 

### Command
```bash
rm -r

```
![typing command in the terminal](rm-r.png)

## 5. cp first.txt okay.txt 

### Description 
The `cp` command for **copy file**, in this example i was copy file first.txt and created same one with name okay.txt

### Command 
```bash
cp

```
![typing command in the terminal](cp.png)

## 6. mv

### Description 
The `mv` command using for two case **for rename** and **for move** file into folder, i show u two option. first example i was change file names okay.txt on 222.txt, in second example i moved file 222.txt into directive 111.

### Command 
```bash
mv

```
![typing command in the terminal](mv1.png)
![typing command in the terminal](mv2.png)

## 7. cat

### Description
The `cat` commands **shows** content file, i actived this command with **file 222.txt** and showed the line with the inscription **wassap***

### Command 
```bash
cat

```
![typing command in the terminal](cat.png)

## 8. less

### Description 
The `less` command shows contents of a file page by page

### Command
```bash
less

```
![typing command in the terminal](less.png)

## 9. head -n 10

### Description 
The `head -n 10` command shows **the first** 10 lines of the file, this value is also changeable, depending on the number of lines u selected to view

### Command
```bash
head -n x

```
![typing command in the terminal](head-n.png)

## 10. tail -n 10 

### Description 
The `tail -n 10` command **shows** the last 10 lines of the file, this value is also changeable depending on the number og lines u selected to view

### Command 
```bash
tail -n x

```
![typing command in the terminal](tail-n.png)

## 11. tail -f 

### Desription 
The `tail -f` command monitoring the file **in real time**

### Command 
```bash
tail -f

```
![typing command in the file](tial-f.png)
